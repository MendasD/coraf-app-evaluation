[project]
name = "coraf-eval"
version = "0.1.0"
description = "Application d'évaluation des performances — CORAF"
requires-python = ">=3.12"
dependencies = [
    "django==5.1.4",
    "psycopg[binary]==3.2.3",
    "python-dotenv==1.0.1",
    "django-htmx==1.21.0",
    "dj-database-url==2.3.0",
    "whitenoise==6.8.2",
    "gunicorn==23.0.0",
]

[tool.uv]
package = false
